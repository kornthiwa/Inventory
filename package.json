{
  "name": "inventory-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "cd web && yarn install && cd ../backend && yarn install",
    "dev:web": "cd web && yarn dev",
    "dev:api": "cd backend && yarn start:dev"
  },
  "engines": {
    "node": ">=20.11.1"
  }
}
